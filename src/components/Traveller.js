export default function Traveller({setArticlesShowing}) {
    function handleNextPage

        return (
        <section className="article-card">
            <button className='action-button'>Previous</button>
            <button className='action-button'>Next</button>
        </section>
        )
}